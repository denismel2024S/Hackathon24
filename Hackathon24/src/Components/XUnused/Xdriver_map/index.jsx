import React, { useState } from "react";
import MapWithDirections from "../map_with_directions";

const DriverMap = ({name, phone, location, destination}) => {


    return(
        <div>
            <MapWithDirections/>
        </div>
    )
}